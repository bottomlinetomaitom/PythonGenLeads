# Sovereign Lead Engine — dependencies
# Install: pip install -r requirements.txt

# Core
aiohttp>=3.9.0,<4.0
beautifulsoup4>=4.12.0,<5.0

# AI (optional — engine works without this via keyword heuristics)
# Install Ollama separately: https://ollama.com
ollama>=0.2.0

# Optional: faster HTML parser (drop-in for beautifulsoup4)
# pip install lxml
# then change "html.parser" to "lxml" in extract_text()
